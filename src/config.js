/**
 * Version of the extension
 * @type {string}
 */
export const ExtensionVersion = "0.1.0-indev";

/**
 * Base URL of where the game files are located
 * @type {string}
 */
export const BaseUrl = "http://classic.margonem.pl";

/**
 * Which endpoints should be redirected to the Merrie server
 * @type {string[]}
 */
export const RedirectEndpoints = [
    "/engine"
];

/**
 * URL of the extension master server
 * @type {string}
 */
export const MasterServerUrl = "https://extension.merrie.pl";